[package]
name = "locus-coordination"
description = "Les agrégats de coordination de SPEC_V1 §7.1 : AgentTemplate, AgentInstance, Team, Decision, ApprovalRequest."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
locus-budget = { path = "../budget" }
locus-domain = { path = "../domain" }
locus-protocol = { path = "../protocol" }

# `locus-review` n'est utilisé que par `tests/visibility.rs`, qui éprouve la sorte `visibility` de
# bout en bout contre la vraie `ContextView`. Le crate de production ne dépend pas de la revue :
# c'est un port qui les relie, et c'est le test qui le branche.
[dev-dependencies]
locus-review = { path = "../review" }
