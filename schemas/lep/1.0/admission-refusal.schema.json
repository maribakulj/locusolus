{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:locus:schema:lep:1.0:admission-refusal",
  "title": "AdmissionRefusal",
  "description": "Pourquoi une mission n'a pas été admise sur un hôte — SPEC_V1 §10.2, ADR 0017 §5.2, tranche 2 du mineur `lep/1.1`. Document **nouveau** : aucune énumération existante ne gagne un membre, ce que l'interdit 3 de l'ADR refuse. Il porte des données et pas seulement des codes — le niveau exigé, le meilleur niveau prouvé, le genre d'accélérateur — donc un membre de plus sur une énumération n'aurait de toute façon pas suffi.",
  "x-since": "1.1",
  "type": "object",
  "required": ["protocol", "task_id", "attempt_id", "reasons"],
  "properties": {
    "protocol": {
      "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/protocol_version"
    },
    "task_id": {
      "type": "string",
      "minLength": 1
    },
    "attempt_id": {
      "type": "string",
      "minLength": 1
    },
    "reasons": {
      "description": "**Toutes** les conditions manquantes, jamais la première seule. `admit` les accumule et rend `Refused { reasons }` au pluriel ; un fil qui n'en transmettrait qu'une ferait corriger une condition pour retomber aussitôt sur la suivante, autant de fois qu'il en manque. `minItems: 1` parce qu'un refus sans motif n'est pas un refus.",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/reason"
      }
    }
  },
  "definitions": {
    "reason": {
      "description": "Les neuf motifs. Écrits **inline** parce qu'une variante n'a pas d'existence hors de son union : la nommer ailleurs produirait une structure autonome que personne n'instancie, et deux endroits à corriger le jour où la forme change. Le compte a monté deux fois depuis ADR 0017 §5.2, qui en nommait six — `disk_quota_not_enforceable` avec W5.g et W5.j, puis les deux motifs de mécanisme avec W5.ag. C'est possible sans toucher à l'interdit 3 de l'ADR parce que ce document est **né** dans le mineur `1.1` : aucun consommateur `1.0` ne le reçoit, donc aucune énumération que `1.0` connaît ne gagne de membre.",
      "oneOf": [
        {
          "description": "L'hôte ne sait pas confiner aussi fort que la mission l'exige. **Distinct de `level_not_attested`** : « l'hôte ne sait pas faire » envoie chercher une autre machine ; « l'hôte l'annonce sans l'avoir prouvé » envoie faire tourner une campagne de self-tests. Les fondre ferait acheter du matériel pour un problème d'attestation.",
          "type": "object",
          "required": ["code", "required", "best"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "level_unavailable"
            },
            "required": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            },
            "best": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            }
          }
        },
        {
          "description": "La réservation dépasse la capacité de l'hôte. Le seul motif sans donnée : ce qui manque est du volume, et la réservation refusée est déjà dans la mission.",
          "type": "object",
          "required": ["code"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "capacity_exceeded"
            }
          }
        },
        {
          "description": "L'accélérateur demandé n'est pas sur cet hôte.",
          "type": "object",
          "required": ["code", "kind"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "accelerator_unavailable"
            },
            "kind": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        {
          "description": "L'hôte ne sait pas **borner** l'espace disque, quel qu'il en reste. Distinct de `capacity_exceeded`, et la distinction n'est pas cosmétique : « la capacité manque » envoie libérer de la place ou réduire la réservation ; « la borne n'est pas applicable ici » envoie changer de système de fichiers, ou de machine. Les fondre ferait réduire une réservation qui aurait échoué de la même façon à un octet. Né avec W5.g et W5.j, après l'écriture d'ADR 0017 §5.2 — qui en nommait six.",
          "type": "object",
          "required": ["code", "requested", "why"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "disk_quota_not_enforceable"
            },
            "requested": {
              "type": "integer",
              "minimum": 0
            },
            "why": {
              "type": "string",
              "minLength": 1
            }
          }
        },
        {
          "description": "L'hôte ne sait pas appliquer ce mode réseau.",
          "type": "object",
          "required": ["code", "mode"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "network_mode_unsupported"
            },
            "mode": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/network_mode"
            }
          }
        },
        {
          "description": "L'hôte annonce ce niveau mais ne l'a jamais prouvé — §12.2 demande une sandbox « disponible **et attestée** ». `proven` est **absent** quand aucune campagne n'a conclu, et ce n'est pas la même ignorance qu'un niveau prouvé trop bas : l'une envoie lancer les self-tests, l'autre dit que l'hôte a échoué à les passer.",
          "type": "object",
          "required": ["code", "required"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "level_not_attested"
            },
            "required": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            },
            "proven": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            }
          }
        },
        {
          "description": "Une campagne a conclu pour cet hôte et ce worker, mais sous un mécanisme que le worker n'emploie pas — ADR 0035 décision 3. **Distinct de `level_not_attested`**, et la distinction est tout l'intérêt : « aucune campagne n'a conclu » envoie lancer les self-tests, celui-ci envoie en lancer une **autre**, sous le mécanisme que ce worker emploie réellement. Les fondre ferait relancer indéfiniment une campagne qui conclut déjà, et bien. Les deux noms sont au registre `schemas/lep/1.0/mechanisms.json` et ils désignent deux mécanismes différents : ADR 0035 et ADR 0036 les tiennent pour incomparables faute de les avoir mesurés l'un contre l'autre.",
          "type": "object",
          "required": ["code", "required", "employs", "attested"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "mechanism_not_employed"
            },
            "required": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            },
            "employs": {
              "description": "Le mécanisme que le manifeste du worker annonce.",
              "type": "string",
              "minLength": 1
            },
            "attested": {
              "description": "Les mécanismes sous lesquels une campagne a conclu pour ce worker, et qui ont été écartés faute de correspondre. Au pluriel : plusieurs campagnes peuvent avoir déposé, et n'en nommer qu'une ferait chercher la mauvaise.",
              "type": "array",
              "minItems": 1,
              "items": { "type": "string", "minLength": 1 }
            }
          }
        },
        {
          "description": "Le mécanisme attesté et celui du worker n'ont pas pu être **rapprochés**, faute d'un nom que le registre connaisse. Distinct de `mechanism_not_employed` : là, les deux noms sont connus et diffèrent ; ici, on ne sait pas ce qu'un nom désigne, et « ce n'est pas le même » serait une affirmation qu'on n'a pas les moyens de faire. `employs` est **absent** quand le manifeste ne nomme aucun mécanisme — `backend` est facultatif dans `CapabilityManifestSandbox` alors qu'il est obligatoire dans `SandboxAttestation` —, et ce n'est pas la même ignorance qu'un nom présent mais hors registre : l'une envoie faire annoncer son mécanisme au worker, l'autre envoie ajouter le nom au registre ou corriger l'émetteur. `unregistered` peut donc être **vide**, et il l'est exactement quand le défaut est du côté de l'annonce.",
          "type": "object",
          "required": ["code", "required", "unregistered"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "mechanism_unresolved"
            },
            "required": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            },
            "employs": {
              "description": "Le mécanisme que le manifeste annonce, quand il en annonce un.",
              "type": "string",
              "minLength": 1
            },
            "unregistered": {
              "description": "Les noms que le registre ne connaît pas, celui du manifeste comme ceux des attestations.",
              "type": "array",
              "items": { "type": "string", "minLength": 1 }
            }
          }
        },
        {
          "description": "L'accélérateur **est** sur cet hôte, mais pas là où la mission veut être confinée. Distinct d'`accelerator_unavailable` : le dire « absent » enverrait chercher du matériel au lieu de choisir entre le conteneur et l'accélérateur.",
          "type": "object",
          "required": ["code", "kind", "required", "native_level"],
          "additionalProperties": false,
          "properties": {
            "code": {
              "const": "accelerator_outside_sandbox"
            },
            "kind": {
              "type": "string",
              "minLength": 1
            },
            "required": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            },
            "native_level": {
              "$ref": "urn:locus:schema:lep:1.0:vocabulary#/definitions/sandbox_level"
            }
          }
        }
      ]
    }
  }
}
