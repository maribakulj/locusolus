{
  "_fixture": {
    "name": "deployment-inline-secret",
    "expect": "invalid",
    "pairs_with": "deployment-single-node-vm.json",
    "note": "Un mot de passe en clair là où une référence est attendue. Un secret dans un fichier de configuration finit dans un dépôt, dans une sauvegarde et dans un rapport de bug — et rien de tout cela ne se révoque."
  },
  "profile": "single-node-vm",
  "endpoint": "https://locus.example/api",
  "adapters": [
    {
      "role": "event-store",
      "implementation": "postgres-16"
    },
    {
      "role": "workflow",
      "implementation": "temporal-oss"
    },
    {
      "role": "object-store",
      "implementation": "minio"
    },
    {
      "role": "sandbox",
      "implementation": "podman-rootless"
    }
  ],
  "capabilities": [
    "cpu",
    "no-gpu"
  ],
  "secret_refs": [
    {
      "name": "event-store.password",
      "reference": "hunter2"
    }
  ]
}
