{
  "name": "locusolus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Locus Solus — laboratoire et control plane. Monorepo.",
  "engines": {
    "node": ">=22.18.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "check": "npm run check:format && npm run check:repo && npm run check:naming && npm run check:roadmap && npm run check:boundaries && npm run check:emacs && npm run check:schemas && npm run check:generated && npm run typecheck && npm test && npm run check:rust",
    "check:rust": "cargo fmt --all --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-features",
    "check:format": "prettier --check .",
    "check:repo": "node tooling/repo/check-repo.ts",
    "check:naming": "node tooling/repo/check-naming.ts",
    "check:roadmap": "node tooling/repo/check-roadmap.ts",
    "check:boundaries": "node tooling/boundaries/check-boundaries.ts",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/**/*.test.ts\"",
    "check:schemas": "node tooling/schemas/check-schemas.ts",
    "sdk": "node tooling/sdk/generate.ts",
    "check:generated": "node tooling/sdk/generate.ts --check",
    "check:emacs": "node tooling/emacs/run-tests.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
