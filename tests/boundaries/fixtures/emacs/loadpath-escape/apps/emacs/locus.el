;;; locus.el --- cockpit Locus Solus  -*- lexical-binding: t; -*-
;;; Commentary:
;; Charge sans erreur, et a quand même franchi la frontière.
;;; Code:

(add-to-list 'load-path (expand-file-name "~/.emacs.d/lisp"))

(provide 'locus)
;;; locus.el ends here
