;;; locus-events.el --- flux d'événements  -*- lexical-binding: t; -*-
;;; Commentary:
;; Compagnon de locus.el, trouvé par la seule load-path du package.
;;; Code:

(defvar locus-events--queue nil)

(provide 'locus-events)
;;; locus-events.el ends here
