# Claude instructions — apps/emacs

Ce dépôt doit rester générique. Aucun chemin ou secret personnel. Toutes les mutations passent par l’API Locus. 3D via projection web + xwidget/WebView si disponible, fallback browser. Les contenus actifs non fiables ne sont pas évalués dans un contexte Emacs privilégié.
